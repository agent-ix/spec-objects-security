manifest_version: 1.0.0
name: spec-objects-security
version: 0.2.0
description: 'Filament Module: tier-2 security and identity ObjectTypes (threats, controls, auth flows, secrets, policies)'
semantic:
  contract_version: 1.0.0
  semantic_core: 0.1.0
  package: agent-ix/spec-objects-security
  exports:
  - auth_flow
  - permission
  - scope
  - role
  - secret
  - encryption_key
  - session_config
  - data_classification
  - trust_boundary
  - audit_event
  - csrf_token
  - cors_policy
  - password_policy
  - mfa_method
  - jwt_claim
  - threat
  - control
  - risk
  - vulnerability
  - asset
  - attack_surface
  - policy
  - audit_finding
  imports: {}
  targets: [json-schema, markdown]
  mappings: [typed-table, sysml-fence, ocl-clause]
  compatibility_posture: additive
  legacy_forms: warning
nav:
  category:
    slug: security
    label: Security
    icon: ShieldCheck
    order: 54
archetypes: []
grammars: []
artifact_types: []
lint_rules: []
object_types:
- name: auth_flow
  roles: [externally-exposed]
  allowed_links:
    requires: [mfa_method]
    grants: [role]
    protects: [externally-exposed]
    references: [auth_flow]
  data_schema:
    schema: schemas/AuthFlow.json
    digest: sha256:e3030e70b3b0d2e31172f9fbc6871f3eaec7ef8507c209574894471be1cd53a4
  body_extraction:
    yield_pattern:
      match:
        id: &id001
          from: frontmatter_field
          path:
          - id
          required: true
        title: &id002
          from: frontmatter_field
          path:
          - title
          required: true
        type: &id003
          from: frontmatter_field
          path:
          - type
          required: true
        flow:
          from: code_block
          after_heading: Flow
          required: true
          language: mermaid
        # Added at 0.2.0 (FR-005): the `## Operations` subsections.
        operations:
          from: section_body
          after_heading: Operations
          required: false
- name: permission
  allowed_links:
    grants: [scope]
    guards: [externally-exposed]
    references: [role]
  data_schema:
    schema: schemas/Permission.json
    digest: sha256:09b8489e0ebb72cbcb41baa09a6d60b17ea69cce07cb0754e934318de044afe9
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        resource:
          from: frontmatter_field
          path:
          - resource
          required: true
        verb:
          from: frontmatter_field
          path:
          - verb
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: scope
  allowed_links:
    references: [permission]
  data_schema:
    schema: schemas/Scope.json
    digest: sha256:67c4a70970add48a21b5764a93dff8a5946401c990d734a7f7f91ae7cbd4fcf5
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        grants:
          from: section_body
          after_heading: Grants
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: role
  allowed_links:
    grants: [permission]
    references: [role]
  data_schema:
    schema: schemas/Role.json
    digest: sha256:ac4125ef0aae3f9c0bbc7f8708f8213de2d74850bf2f8639c79f33bce0d80974
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        permissions:
          from: section_body
          after_heading: Permissions
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: secret
  roles: [sensitive]
  allowed_links:
    references: [secret]
  data_schema:
    schema: schemas/Secret.json
    digest: sha256:8db617e54367c6e747b83dbfedb480b6fa5e87b5e2a6584cd7bbf9b294e2d6f7
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        rotation:
          from: section_body
          after_heading: Rotation
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: encryption_key
  roles: [sensitive]
  allowed_links:
    encrypts: [sensitive, data_classification]
  data_schema:
    schema: schemas/EncryptionKey.json
    digest: sha256:439e8b376d8e1dfb391d8e8533916bf0917f5d28f895d4c307b9e0cc37319f14
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        algorithm:
          from: frontmatter_field
          path:
          - algorithm
          required: true
        rotation:
          from: frontmatter_field
          path:
          - rotation
          required: false
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: session_config
  allowed_links:
    references: [auth_flow, jwt_claim]
  data_schema:
    schema: schemas/SessionConfig.json
    digest: sha256:ac389d764830f73f6baa749bdb39590303ffe0afb49d29db105b908619b10117
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        settings:
          from: section_body
          after_heading: Settings
          required: true
        # Added at 0.2.0 (FR-005): the `## Invariants` OCL clauses.
        invariants:
          from: section_body
          after_heading: Invariants
          required: false
- name: data_classification
  allowed_links:
    classifies: [persistable, externally-exposed, sensitive]
    references: [data_classification]
  data_schema:
    schema: schemas/DataClassification.json
    digest: sha256:82a2b1feb8c8f35d7cac3f44b64aac2228b8037b6e896878dfc9778099e51f61
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        handling:
          from: section_body
          after_heading: Handling
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: trust_boundary
  allowed_links:
    contains: [asset, api_endpoint]
    references: [trust_boundary]
  data_schema:
    schema: schemas/TrustBoundary.json
    digest: sha256:b3917906caebca63485f44d9688498fd7888725390e8c5aa8a5b7ac1c448dab1
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        boundary:
          from: code_block
          after_heading: Boundary
          required: true
          language: mermaid
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
        # Added at 0.2.0 (FR-005): the `## Invariants` OCL clauses.
        invariants:
          from: section_body
          after_heading: Invariants
          required: false
- name: audit_event
  roles: [event-like]
  allowed_links:
    references: [audit_event]
  data_schema:
    schema: schemas/AuditEvent.json
    digest: sha256:0bfa63622e3ab602a00f7e7668224b3590f8fa9e71a8bf4d997daf26c019e87b
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        event_schema:
          from: code_block
          after_heading: Event Schema
          required: true
          language: json
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: csrf_token
  roles: [sensitive]
  allowed_links:
    references: [csrf_token]
  data_schema:
    schema: schemas/CsrfToken.json
    digest: sha256:97867d6d36073d85da4e8cca1cb7fb578a3a5efc4bf8a1f0adf03af2b5f10725
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        rotation_window:
          from: frontmatter_field
          path:
          - rotation_window
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: cors_policy
  allowed_links:
    references: [cors_policy]
  data_schema:
    schema: schemas/CorsPolicy.json
    digest: sha256:24b9e6f16efeb6710aa6529ae8a7408e4814cd96954a646c1e937be7fd6a9bd3
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        origins:
          from: section_body
          after_heading: Origins
          required: true
        # Added at 0.2.0 (FR-005): the `## Invariants` OCL clauses.
        invariants:
          from: section_body
          after_heading: Invariants
          required: false
- name: password_policy
  allowed_links:
    references: [password_policy]
  data_schema:
    schema: schemas/PasswordPolicy.json
    digest: sha256:f16db992b0eafbf60ff7274c8a2a06965c29df3912ac7b81b3d2272e3e08aace
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        rules:
          from: section_body
          after_heading: Rules
          required: true
        # Added at 0.2.0 (FR-005): the `## Invariants` OCL clauses.
        invariants:
          from: section_body
          after_heading: Invariants
          required: false
- name: mfa_method
  allowed_links:
    references: [mfa_method]
  data_schema:
    schema: schemas/MfaMethod.json
    digest: sha256:7ba44d012e50785e6a66cab42dc1a198e9813357e3577f5331f3819196a92dac
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        kind:
          from: frontmatter_field
          path:
          - kind
          required: true
        factor:
          from: frontmatter_field
          path:
          - factor
          required: false
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: jwt_claim
  roles: [sensitive]
  allowed_links:
    references: [jwt_claim]
  data_schema:
    schema: schemas/JwtClaim.json
    digest: sha256:9ebbffee53b0eb5842f713c2009228797674f21553530531ac3211cc3e4cfb91
  body_extraction:
    yield_pattern:
      match:
        id: *id001
        title: *id002
        type: *id003
        schema_json:
          from: code_block
          after_heading: Schema
          required: true
          language: json
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: threat
  allowed_links:
    threatens: [asset, attack_surface]
    exploits: [vulnerability]
  data_schema:
    schema: schemas/Threat.json
    digest: sha256:2ad6f6cd7d94ca4b7ec0433dfd400b329151147f27ba85d67337fe82012744e9
  body_extraction:
    yield_pattern:
      match:
        id: &id004
          from: frontmatter_field
          path:
          - id
          required: true
        title: &id005
          from: frontmatter_field
          path:
          - title
          required: true
        type: &id006
          from: frontmatter_field
          path:
          - type
          required: true
        stride_category:
          from: frontmatter_field
          path:
          - stride_category
          required: true
        vector:
          from: frontmatter_field
          path:
          - vector
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: control
  allowed_links:
    mitigates: [threat, risk, vulnerability]
    implements: [policy]
    protects: [externally-exposed, asset]
  data_schema:
    schema: schemas/Control.json
    digest: sha256:a0f2bdf858dadf719851b730d9349f33bdbe19f5935a6f59b17987151208d843
  body_extraction:
    yield_pattern:
      match:
        id: *id004
        title: *id005
        type: *id006
        mappings:
          from: section_body
          after_heading: Mappings
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
        # Added at 0.2.0 (FR-005): the `## Invariants` OCL clauses.
        invariants:
          from: section_body
          after_heading: Invariants
          required: false
- name: risk
  allowed_links:
    arises_from: [threat, vulnerability]
    references: [asset]
  data_schema:
    schema: schemas/Risk.json
    digest: sha256:084c3267ea01b4c14c7e28d388a61f8ffde1772f6be58a59ed214ce25d619a10
  body_extraction:
    yield_pattern:
      match:
        id: *id004
        title: *id005
        type: *id006
        likelihood:
          from: frontmatter_field
          path:
          - likelihood
          required: true
        impact:
          from: frontmatter_field
          path:
          - impact
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: vulnerability
  allowed_links:
    affects: [asset, attack_surface]
    references: [vulnerability]
  data_schema:
    schema: schemas/Vulnerability.json
    digest: sha256:58c693701bf113ba6f3111f9ed783280b3108093d542ca2805d13c6c5ebdf4a4
  body_extraction:
    yield_pattern:
      match:
        id: *id004
        title: *id005
        type: *id006
        cve_id:
          from: frontmatter_field
          path:
          - cve_id
          required: false
        severity:
          from: frontmatter_field
          path:
          - severity
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: asset
  allowed_links:
    references: [data_classification, trust_boundary]
  data_schema:
    schema: schemas/Asset.json
    digest: sha256:b2b8c69dfb860eb4ef329bb816fc429fba5b92a8fc1ae90ba21be284d3d96978
  body_extraction:
    yield_pattern:
      match:
        id: *id004
        title: *id005
        type: *id006
        description_body:
          from: section_body
          after_heading: Description
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: attack_surface
  roles: [externally-exposed]
  allowed_links:
    exposes: [asset, api_endpoint]
  data_schema:
    schema: schemas/AttackSurface.json
    digest: sha256:0f98693ab17c019884fc8cc7e12a71c2399e15491746a8cb5a6078d528afbec9
  body_extraction:
    yield_pattern:
      match:
        id: *id004
        title: *id005
        type: *id006
        entry_points:
          from: section_body
          after_heading: Entry Points
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false
- name: policy
  allowed_links:
    governs: [control, role, permission, data_classification]
  data_schema:
    schema: schemas/Policy.json
    digest: sha256:247852d09e05e83ac5e07954b0bcb475e437361c40f4833a617a3d7d964ea464
  body_extraction:
    yield_pattern:
      match:
        id: *id004
        title: *id005
        type: *id006
        policy_body:
          from: section_body
          after_heading: Policy
          required: true
        # Added at 0.2.0 (FR-005): the `## Invariants` OCL clauses.
        invariants:
          from: section_body
          after_heading: Invariants
          required: false
- name: audit_finding
  allowed_links:
    traces_to: [risk]
    references: [control, vulnerability, asset]
  data_schema:
    schema: schemas/AuditFinding.json
    digest: sha256:041c09bb6d16297f751af7e05f88380b3ff8714361a67b3a099f026d7cb24529
  body_extraction:
    yield_pattern:
      match:
        id: *id004
        title: *id005
        type: *id006
        recommendation:
          from: section_body
          after_heading: Recommendation
          required: true
        # Added at 0.2.0 (FR-005): the typed `## Properties` table the
        # semantic layer reads. Optional, so every 0.1.0 artifact stays valid.
        properties:
          from: section_body
          after_heading: Properties
          required: false

# FR-043: concrete-term lexicon — security / identity / auth objects.
# FR-058 (quire-rs v0.31.0): upward-trace completeness — the second half of
# agent-ix/spec-objects-security#5. Declaration, not code: the engine holds no
# archetype name, no verb and no direction.
#
# A threat nobody controls and a risk nobody treats are both perfectly
# well-formed documents that pass every schema and grammar check, and both are
# evidence of an unaddressed exposure. Nothing that reads a document on its own
# can see it; only reading the edge set upward can.
traceability:
  required_relations:
  # STRIDE without coverage is a list. A threat with no inbound `mitigates` is
  # one somebody enumerated and then left.
  - name: threat-has-control
    from: threat
    edges: [mitigates]
    to: []
    direction: incoming
    check: uncontrolled-threat
  # The same statement about the scored artefact rather than the mechanism: a
  # risk with no treatment was assessed and then dropped.
  - name: risk-has-treatment
    from: risk
    edges: [mitigates]
    to: []
    direction: incoming
    check: untreated-risk
  # `direction: incoming` throughout, because the mitigation is authored from
  # the control's or requirement's end — a control says what it mitigates. A
  # threat listing its own controls would duplicate the fact and let the two
  # drift.
  #
  # DELIBERATELY NOT DECLARED: `vulnerability`. #5 names "threat/hazard/risk"
  # and a vulnerability is a property of an implementation rather than a thing
  # the specification owes a response to. Adding it would widen the ticket.
  #
  # ALSO NOT DECLARED: the reverse direction, "every security-classified
  # requirement traces to an upstream threat". It needs a way to say a
  # requirement is security-classified, and no such axis exists — neither this
  # module nor iso declares one. Filed rather than invented.
  #
  # A risk that transitively arises from itself states nothing, and no
  # per-document check can see it: the defect is a property of the graph.
  acyclic_edges:
  - arises_from

lexicon:
  token: {definition: a bearer credential representing identity or grant}
  secret: {definition: "a confidential value (key, password) kept out of code"}
  scope: {definition: a named permission boundary on a grant}
  claim: {definition: an assertion carried in a token}
  permission: {definition: an allowed action on a resource}
  credential: {definition: data proving an identity}
  certificate: {definition: a signed binding of identity to a public key}
  signature: {definition: a cryptographic proof of integrity/origin}
  principal: {definition: an authenticated identity (user/service)}
  keypair: {definition: a public/private cryptographic key pair}
  nonce: {definition: a single-use value preventing replay}
  hash: {definition: a fixed-size digest of input}
  cipher: {definition: an encryption/decryption algorithm}
  audit: {definition: "a recorded, tamper-evident event trail"}
  policy: {definition: a declarative rule governing access or behavior}
